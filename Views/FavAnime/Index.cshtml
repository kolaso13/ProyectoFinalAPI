@model IEnumerable<WebApiAnimeProyecto.Models.Anime_User>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AnimeData)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AnimeData.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FavoritoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FavoritoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FavoritoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
